{
  "/": {
    "carrier": {
      "description": "The name of the carrier to use for the oubound call leg",
      "type": "string",
      "required": true
    },
    "callerId": {
      "description": "Caller ID to use for the outbound leg, if not set then the CallerID of the incomming call will be used",
      "type": "string"
    }
  }}